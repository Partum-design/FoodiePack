# Used only when building the Capacitor Android app (npm run build:mobile).
# The packaged app has no same-origin "/api" to call, so it talks to the
# production API directly. Contains no secrets — safe to commit.
VITE_API_URL=https://www.foodiepack.com.mx/api
